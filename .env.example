# Jarvis Voice Bot Configuration
# Copy this to .env and fill in values

# Discord bot token (MUST be a SEPARATE bot application from Clawdbot!)
# Create at: https://discord.com/developers/applications
# Required intents: GUILD_VOICE_STATES, GUILDS
DISCORD_TOKEN=

# Guild and channel IDs
# Find your guild ID: Right-click server icon → Copy Server ID (Developer Mode must be on)
# Find channel ID: Right-click voice channel → Copy Channel ID
DISCORD_GUILD_ID=
DISCORD_VOICE_CHANNEL_ID=

# ── Clawdbot Gateway (PRIMARY BRAIN) ──────────────────────────────────
# Routes through Claude Max subscription (no API credits needed)
# The voice bot sends transcripts here, Clawdbot does ALL thinking
CLAWDBOT_GATEWAY_URL=http://127.0.0.1:22100
CLAWDBOT_GATEWAY_TOKEN=

# ── Speech-to-Text ───────────────────────────────────────────────────
# "deepgram" (faster, cheaper: $0.0043/min) | "whisper" (fallback: $0.006/min)
STT_PROVIDER=deepgram

# Deepgram API key (get free $200 credit at console.deepgram.com)
DEEPGRAM_API_KEY=

# OpenAI Whisper API (fallback)
OPENAI_API_KEY=

# ── TTS Provider ──────────────────────────────────────────────────────
# "edge" (free, William Australian) | "openai" (paid, higher quality)
TTS_PROVIDER=edge

# Edge TTS settings (free)
EDGE_TTS_VOICE=en-AU-WilliamNeural

# OpenAI TTS settings (optional, paid)
OPENAI_TTS_MODEL=gpt-4o-mini-tts
OPENAI_TTS_VOICE=onyx

# ── Access Control ────────────────────────────────────────────────────
# Allowed Discord user IDs (comma-separated)
# Find your user ID: Settings → Advanced → Developer Mode → Right-click yourself → Copy User ID
ALLOWED_USERS=

# ── Wake Word Detection ──────────────────────────────────────────────
# Set to true to require "Jarvis" at start of speech
# After bot responds, 60s conversation window (no wake word needed)
WAKE_WORD_ENABLED=false
WAKE_WORD_PHRASES=jarvis,hey jarvis,hey travis,yo jarvis
CONVERSATION_WINDOW_MS=60000

# ── Streaming TTS ─────────────────────────────────────────────────────
# Sentence-level chunking for faster time-to-first-word
STREAMING_TTS_ENABLED=true

# ── User Configuration ────────────────────────────────────────────────
# Session identifier for Clawdbot (used for context separation)
SESSION_USER=jarvis-voice-user
# Discord channel ID for context fetching (optional, for thread awareness)
DISCORD_CHANNEL_ID=
